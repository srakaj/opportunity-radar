name: Daily Opportunity Radar

on:
  workflow_dispatch:
  schedule:
    - cron: '15 5 * * *'
  push:
    branches:
      - main
    paths:
      - 'radar/**'
      - 'scripts/**'
      - 'config/**'
      - 'requirements.txt'
      - '.github/workflows/daily-radar.yml'

permissions:
  contents: write

jobs:
  run-radar:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run radar
        run: python scripts/run_radar.py

      - name: Commit updated opportunity data
        run: |
          if git diff --quiet -- data/opportunities.json docs/opportunities.json; then
            echo "No data changes"
            exit 0
          fi
          git config user.name "opportunity-radar[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/opportunities.json docs/opportunities.json
          git commit -m "Update opportunity radar data"
          git push
